/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'localhost',
      'digitalhippo-production-bfac.up.railway.app'
    ]
  },
};

export default nextConfig;
